import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jeremycrandall.aihalloween',
  appName: 'AI Halloween',
  webDir: 'dist'
};

export default config;
